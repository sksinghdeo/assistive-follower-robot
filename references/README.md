# References and Source Material

This folder stores the original project notes and scripts used to build the polished portfolio repository.

- `source_documents/` contains the project methodology documents and TurtleBot UI PDF.
- `original_scripts/` contains the original training/import/live prediction scripts before cleanup.

The main repo uses cleaned versions under `tools/` and `assistive_follower_robot/`.
